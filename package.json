{
  "name": "null0",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:example:assemblyscript": "asc examples/assemblyscript/demo.ts --target debug --bindings raw",
    "watch:example:assemblyscript": "npx -y onchange -v -i examples/assemblyscript sources/assemblyscript -- npm run build:example:assemblyscript",
    "watch:target:web": "npx -y live-server targets/web",
    "build:target:web": "asc examples/assemblyscript/demo.ts --target release --bindings raw",
    "start": "run-p watch:example:assemblyscript watch:target:web"
  },
  "keywords": [],
  "author": "David Konsumer <konsumer@jetboystudio.com>",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "assemblyscript": "^0.20.4"
  }
}
